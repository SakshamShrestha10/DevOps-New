apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devopswebapp.fullname" . }}-test-connection"
  labels:
    {{- include "devopswebapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devopswebapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
